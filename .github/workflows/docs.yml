name: Publish Documentation to sites.ecmwf.int
on:
  pull_request:
    types: [opened, synchronize, reopened, closed]
  push:
    branches:
      - "develop"
      - "main"
    tags:
      - "**"
jobs:
  build:
    if: ${{ github.event_name == 'pull_request' && github.event.action != 'closed' || github.event_name == 'push'}}
    runs-on: ubuntu-latest
    outputs:
      artifact-id: ${{ steps.upload-doc.outputs.artifact-id }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: 3.13
      - name: Build and Install
        run: |
          python -m pip install ".[dev,docs,cbor]"
      - name: Test
        run: |
          sphinx-build docs docs/_build
      - name: Archive documentation
        id: upload-doc
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: documentation
          path: docs/_build
  preview-publish:
    if: ${{ github.event_name == 'pull_request' && github.event.action != 'closed' }}
    needs: build
    uses: ecmwf/reusable-workflows/.github/workflows/pr-preview-publish.yml@6bb439f3a1f1f053e862993b0814e50db3eec1c9 # main
    with:
      artifact-id: ${{ needs.build.outputs.artifact-id }}
      space: docs
      name: dev-section
      path: qubed/pull-requests
      link-text: 🌈🌦️📖🚧 Documentation 🚧📖🌦️🌈
    secrets:
      sites-token: ${{ secrets.ECMWF_SITES_DOCS_DEV_SECTION_TOKEN }}
  preview-unpublish:
    if: ${{ github.event_name == 'pull_request' && github.event.action == 'closed' }}
    uses: ecmwf/reusable-workflows/.github/workflows/pr-preview-unpublish.yml@6bb439f3a1f1f053e862993b0814e50db3eec1c9 # main
    with:
      space: docs
      name: dev-section
      path: qubed/pull-requests
    secrets:
      sites-token: ${{ secrets.ECMWF_SITES_DOCS_DEV_SECTION_TOKEN }}
  publish:
    if: >-
      ${{ github.event_name == 'push' && (
        github.ref_name == 'develop' ||
        github.ref_name == 'main'  ||
        github.ref_type == 'tag'
      ) }}
    needs: build
    uses: ecmwf/reusable-workflows/.github/workflows/docs-publish.yml@6bb439f3a1f1f053e862993b0814e50db3eec1c9 # main
    with:
      artifact-id: ${{ needs.build.outputs.artifact-id }}
      space: docs
      name: dev-section
      path: qubed
      id: ${{ github.ref_name }}
      softlink: ${{ github.ref_name == 'main'   && 'stable'
        || github.ref_name == 'develop' && 'latest' }}
    secrets:
      sites-token: ${{ secrets.ECMWF_SITES_DOCS_DEV_SECTION_TOKEN }}
